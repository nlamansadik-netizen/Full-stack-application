version: '3.9'

services:
  mongo:
    image: mongo:7
    container_name: intern-mongo
    restart: unless-stopped
    ports:
      - '27017:27017'
    environment:
      MONGO_INITDB_DATABASE: intern_story_app
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
